<Window x:Class="CapturePlus.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置" Width="500" Height="440" WindowStartupLocation="CenterScreen"
        Background="#FFF3F4F6" UseLayoutRounding="True">
    <Window.Resources>
        <LinearGradientBrush x:Key="BtnBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Offset="0" Color="#FFFFFF"/>
            <GradientStop Offset="1" Color="#E6E8EB"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="BtnHoverBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Offset="0" Color="#FCFDFE"/>
            <GradientStop Offset="1" Color="#DEE1E5"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="BtnPressedBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Offset="0" Color="#D5D8DC"/>
            <GradientStop Offset="1" Color="#C8CBCF"/>
        </LinearGradientBrush>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#2B2B2B"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{StaticResource BtnBrush}"
                                BorderBrush="#B9BCC2" BorderThickness="1" CornerRadius="4">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="3" ShadowDepth="2" Opacity="0.28" Color="#444"/>
                            </Border.Effect>
                            <Border CornerRadius="3" Margin="1" BorderThickness="0,1,0,0" BorderBrush="#55FFFFFF">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}" RecognizesAccessKey="True"/>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BtnHoverBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#9CA0A6"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BtnPressedBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#85898F"/>
                                <Setter TargetName="Bd" Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="4,3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="CaretBrush" Value="#333"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" CornerRadius="3" BorderThickness="1"
                                BorderBrush="#B9BCC2" Background="White">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="3,1"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#8F939A"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#4D90FE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="4,3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="CaretBrush" Value="#333"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="Bd" CornerRadius="3" BorderThickness="1"
                                BorderBrush="#B9BCC2" Background="White">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="3,1"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#8F939A"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#4D90FE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Chrome" CornerRadius="3" BorderThickness="1"
                                                BorderBrush="#B9BCC2" Background="White">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="2" ShadowDepth="1" Opacity="0.18"/>
                                            </Border.Effect>
                                            <Path Data="M0,0 L10,0 L5,6 Z" Fill="#5A5E65"
                                                  HorizontalAlignment="Right" VerticalAlignment="Center"
                                                  Margin="0,0,9,0" IsHitTestVisible="False"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#8F939A"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Chrome" Property="Background" Value="#EFF1F4"/>
                                                <Setter TargetName="Chrome" Property="Effect" Value="{x:Null}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" Margin="6,0,26,0"
                                              HorizontalAlignment="Left" VerticalAlignment="Center"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              IsHitTestVisible="False"/>
                            <TextBox x:Name="PART_EditableTextBox" Margin="6,0,26,0" Padding="0"
                                     VerticalAlignment="Center" Visibility="Hidden" Focusable="True"
                                     Background="Transparent" BorderThickness="0"
                                     Foreground="#333" Style="{x:Null}"/>
                            <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom" AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Fade">
                                <Border x:Name="DropDownBorder" MinWidth="100"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        SnapsToDevicePixels="True" Background="White"
                                        BorderBrush="#B9BCC2" BorderThickness="1"
                                        CornerRadius="4" Margin="0,2,0,0">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="6" ShadowDepth="2" Opacity="0.25"/>
                                    </Border.Effect>
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" CornerRadius="3" Margin="2,1" Padding="{TemplateBinding Padding}"
                                Background="Transparent">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#EAF2FE"/>
                                <Setter Property="Foreground" Value="#1A6FE0"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#DCE9FB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Style="{StaticResource SectionHeader}" Text="AI 服务"/>

            <Grid Grid.Row="1" Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Content="BaseUrl:"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="BaseUrlBox" Margin="0,2"/>
                <Label Grid.Row="1" Content="ApiKey:"/>
                <Grid Grid.Row="1" Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" x:Name="ApiKeyBox" Margin="0,2"
                             VerticalAlignment="Center" Visibility="Collapsed"
                             TextChanged="OnApiKeyChanged"/>
                    <PasswordBox Grid.Column="0" x:Name="ApiKeyPwd" Margin="0,2"
                                 PasswordChanged="OnApiKeyPwdChanged"/>
                    <Button Grid.Column="1" x:Name="ToggleKeyBtn" Content="👁" Width="32"
                            Margin="4,0,0,0" Click="OnToggleKey"/>
                </Grid>
                <Label Grid.Row="2" Content="视觉模型:"/>
                <TextBox Grid.Row="2" Grid.Column="1" x:Name="VisionModelBox" Margin="0,2"/>
                <Label Grid.Row="3" Content="文本模型:"/>
                <TextBox Grid.Row="3" Grid.Column="1" x:Name="TextModelBox" Margin="0,2"/>
                <Button Grid.Row="4" Grid.Column="1" Content="测试连接" HorizontalAlignment="Left"
                        Padding="12,4" Margin="0,6,0,0" Click="OnTestConnection"/>
            </Grid>

            <TextBlock Grid.Row="2" Style="{StaticResource SectionHeader}" Text="翻译"/>
            <Grid Grid.Row="3" Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Content="目标语言:"/>
                <ComboBox Grid.Column="1" x:Name="TranslateLangBox" Margin="0,2"
                          IsEditable="True"/>
            </Grid>

            <Grid Grid.Row="4" Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Content="保存目录:"/>
                <TextBox Grid.Column="1" x:Name="SaveDirBox" Margin="0,2"/>
                <Button Grid.Column="2" Content="浏览…" Margin="4,0,0,0" Padding="8,4" Click="OnBrowse"/>
            </Grid>

            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <TextBlock x:Name="HotkeyLabel" Text="快捷键: Ctrl+Alt+A" VerticalAlignment="Center" Margin="0,0,16,0" Foreground="Gray"/>
                <Button Content="保存" Padding="16,6" Margin="0,0,8,0" Click="OnSave"/>
                <Button Content="取消" Padding="16,6" Click="OnCancel"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>
